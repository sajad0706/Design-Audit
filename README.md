# Design-Audit
Figma plugin that audits your design against production code, detecting style mismatches, missing styles, and token differences. It adds clear annotations on affected layers so designers can quickly fix inconsistencies and ensure design matches production.
